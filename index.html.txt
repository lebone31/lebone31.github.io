<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Birthday Charlie 🤍</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500&display=swap');

body {
  margin: 0;
  height: 100vh;
  background: #000;
  color: #fff;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: 'Playfair Display', serif;
  overflow: hidden;
}

.page {
  display: none;
  text-align: center;
  padding: 20px;
}

.page.active {
  display: block;
}

/* TEXT ABOVE CAKE */
.title {
  font-size: 22px;
  letter-spacing: 1px;
  margin-bottom: 20px;
}

/* CAKE */
.cake {
  width: 200px;
  margin: 0 auto 20px;
}

.cake img {
  width: 100%;
  filter: grayscale(100%);
}

/* CANDLES */
.candles {
  font-size: 24px;
  margin-top: -30px;
}

.candle.on {
  animation: flicker 1s infinite;
}

@keyframes flicker {
  0% { opacity: 1; }
  50% { opacity: 0.6; }
  100% { opacity: 1; }
}

/* BUTTON */
button {
  margin-top: 20px;
  padding: 10px 20px;
  border-radius: 20px;
  border: none;
  background: #fff;
  color: #000;
  cursor: pointer;
}

/* ENVELOPE */
.envelope {
  font-size: 90px;
  cursor: pointer;
  animation: float 1.5s infinite;
}

@keyframes float {
  0% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0); }
}

/* LETTER PAGE FLIP */
.book {
  perspective: 1000px;
}

.page-flip {
  width: 320px;
  background: #111;
  padding: 20px;
  transform-origin: left;
  animation: flip 1.2s forwards;
}

@keyframes flip {
  from { transform: rotateY(-90deg); }
  to { transform: rotateY(0deg); }
}

/* LETTER */
.letter {
  line-height: 1.8;
  font-size: 16px;
}

/* HEARTS */
.hearts {
  position: fixed;
  inset: 0;
  pointer-events: none;
}

.heart {
  position: absolute;
  bottom: -20px;
  font-size: 28px;
  animation: floatUp 5s linear forwards;
}

@keyframes floatUp {
  from { transform: translateY(0); opacity: 1; }
  to { transform: translateY(-100vh); opacity: 0; }
}

/* BURST */
.burst {
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  font-size: 26px;
  animation: burst 1s ease-out forwards;
}

@keyframes burst {
  from { transform: translate(-50%, -50%) scale(0.4); opacity: 1; }
  to {
    transform: translate(
      calc(-50% + var(--x)),
      calc(-50% + var(--y))
    ) scale(1.4);
    opacity: 0;
  }
}
</style>
</head>

<body>

<!-- PAGE 1: CAKE -->
<div class="page active" id="cakePage">
  <div class="title">HAPPY BIRTHDAY CHARLIE 🤍</div>

  <div class="cake">
    <img src="https://i.imgur.com/8YqGkWa.png" alt="Cake">
    <div class="candles">
      <span class="candle on">🕯️</span>
      <span class="candle on">🕯️</span>
      <span class="candle on">🕯️</span>
    </div>
  </div>

  <button onclick="startMic()">Blow gently</button>
</div>

<!-- PAGE 2: ENVELOPE -->
<div class="page" id="envelopePage">
  <div class="envelope" onclick="openLetter()">✉️</div>
</div>

<!-- PAGE 3: LETTER -->
<div class="page" id="letterPage">
  <div class="book">
    <div class="page-flip">
      <div class="letter">
        Sthandwa 🤍<br><br>

        I know we haven’t known each other for a very long time,
        but somehow you’ve become such an important part of my life.
        A part I didn’t even know I needed.<br><br>

        It’s like you sneaked in, stole a piece of my heart and now
        I’m stuck with you 😗 — and honestly, I wouldn’t have it any
        other way.<br><br>

        I want you to know that you are the cutest, kindest,
        most caring and loving man ever!!!
        I like your smile, I love your voice,
        I just love everything about you!! 🥹
        And I am sooo grateful for you 🥹<br><br>

        I could write a whole book about you,
        but I don’t have much space here…<br><br>

        I pray this new year of your life overflows with everything
        your heart desires and endless happiness ❤️<br><br>

        Have a good one.<br><br>

        Happy Birthday, my love 🤍
      </div>
    </div>
  </div>
</div>

<div class="hearts" id="hearts"></div>

<audio id="music">
  <source src="https://cdn.pixabay.com/download/audio/2022/10/30/audio_1bdf1d9f1b.mp3">
</audio>

<script>
const cakePage = document.getElementById("cakePage");
const envelopePage = document.getElementById("envelopePage");
const letterPage = document.getElementById("letterPage");
const candles = document.querySelectorAll(".candle");
const heartsDiv = document.getElementById("hearts");
const music = document.getElementById("music");

function startMic() {
  navigator.mediaDevices.getUserMedia({ audio: true }).then(stream => {
    const ctx = new AudioContext();
    const mic = ctx.createMediaStreamSource(stream);
    const analyser = ctx.createAnalyser();
    mic.connect(analyser);
    analyser.fftSize = 256;

    const data = new Uint8Array(analyser.frequencyBinCount);

    function listen() {
      analyser.getByteFrequencyData(data);
      let volume = data.reduce((a,b)=>a+b)/data.length;
      if (volume > 40) {
        candles.forEach(c => c.classList.remove("on"));
        softHearts();
        setTimeout(() => {
          cakePage.classList.remove("active");
          envelopePage.classList.add("active");
        }, 1000);
      } else {
        requestAnimationFrame(listen);
      }
    }
    listen();
  });
}

function softHearts() {
  for (let i = 0; i < 10; i++) {
    const h = document.createElement("div");
    h.className = "heart";
    h.textContent = "🤍";
    h.style.left = Math.random() * 100 + "vw";
    heartsDiv.appendChild(h);
    setTimeout(() => h.remove(), 5000);
  }
}

function openLetter() {
  envelopePage.classList.remove("active");
  letterPage.classList.add("active");
  burstHearts();
  music.volume = 0.4;
  music.play();
}

function burstHearts() {
  for (let i = 0; i < 25; i++) {
    const b = document.createElement("div");
    b.className = "burst";
    b.textContent = "🤍";
    b.style.setProperty("--x", (Math.random() - 0.5) * 300 + "px");
    b.style.setProperty("--y", (Math.random() - 0.5) * 300 + "px");
    document.body.appendChild(b);
    setTimeout(() => b.remove(), 1000);
  }
}
</script>

</body>
</html>
